{
	 services = {
	 	libinput.enable = true;
		gvfs.enable = true;
		dbus.enable = true;

		# Enable CUPS to print documents.
		printing.enable = true;

		# Enable the OpenSSH daemon.
		openssh.enable = true;
	};
}
